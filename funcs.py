def sum_numbers(numbers):
    total = 0
    for number in numbers:
        total += number
    return total

def min_numbers(numbers):
    total = 0
    for number in numbers:
        total -= number
    return total
